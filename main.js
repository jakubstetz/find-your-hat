const prompt = require('prompt-sync')({sigint: true});

const characters = {
  hat: '^',
  hole: 'O',
  field: '░',
  path: '*'
}

class Field {
  constructor(input) { // input represents the field.

  }
}